// Icon registry — previously from v-remixicon/icons
// Now uses inline SVG path data stored in this module
// Icons can be added by copying SVG path data from https://remixicon.com/

export const icons: Record<string, string> = {
  // Placeholder — real icon paths should be added as needed
  // Format: 'riIconName': 'M...SVG path data...'
};

// React-compatible icon library — no Vue dependency
export default {
  install: () => { /* no-op in React world */ },
};
